# C1 Breakout v2.6 Trading Bot - Requirements
# For production deployment, install runtime dependencies only:
#   pip install -r requirements/runtime.txt
#
# For development (includes testing, analysis, visualization):
#   pip install -r requirements/dev.txt
#
# Legacy dependencies (archived RL bots) available in:
#   requirements/legacy.txt

# Default: Install runtime dependencies for production
-r requirements/runtime.txt
